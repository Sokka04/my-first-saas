# skoolis_desktop

Client **Electron** : UI, cache local, impression, sync future. Aucune règle métier : tout passe par l’API Laravel.

Initialisation suggérée :

```bash
npm init -y
npm install electron --save-dev
```

Authentification : tokens Sanctum (stockage sécurisé local).
