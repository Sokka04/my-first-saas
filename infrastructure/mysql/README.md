# MariaDB / MySQL

- Encodage : `utf8mb4` / `utf8mb4_unicode_ci`.
- Sauvegardes : voir `infrastructure/backups/`.
- Indexation : toujours indexer `school_id` et, pour les données scolaires, `school_year_id`.
